name: Stage 6 production deploy

on:
  push:
    branches: [stage-4-hosted-qa]

permissions:
  contents: read

env:
  SOURCE_SHA: 2efd033b33efecdaf87d5f37ae7f12a64397e849
  VERCEL_ORG_ID: team_7hPovBT2iV7GpndA5ssdCTZs
  VERCEL_PROJECT_ID: prj_rMjJIycxQZmCSvPpupYs5PsbqFjm

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact validated main
        uses: actions/checkout@v4
        with:
          ref: ${{ env.SOURCE_SHA }}

      - name: Verify source SHA
        run: test "$(git rev-parse HEAD)" = "$SOURCE_SHA"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Revalidate exact source
        run: |
          npm install --no-audit --no-fund
          npm run validate:data
          npm run validate:ui
          npm run build

      - name: Verify Vercel credential is available
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "VERCEL_TOKEN secret is not configured"
            exit 2
          fi

      - name: Deploy exact validated main to production project
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          mkdir -p .vercel
          printf '{"orgId":"%s","projectId":"%s"}\n' "$VERCEL_ORG_ID" "$VERCEL_PROJECT_ID" > .vercel/project.json
          npm install --global vercel@59.16.0 --no-audit --no-fund
          URL="$(vercel deploy --prod --yes --token="$VERCEL_TOKEN")"
          echo "STAGE6_PRODUCTION_URL=$URL" | tee stage6-production-url.txt
          case "$URL" in
            *trek-jacket-finder*) ;;
            *) echo "Unexpected production deployment URL: $URL"; exit 1 ;;
          esac

      - name: Upload production deployment URL
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage6-production-deployment-url
          path: stage6-production-url.txt
          if-no-files-found: ignore
          retention-days: 2
