name: Stage 4 hosted staging QA legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Stage 4 diagnostic workflow parked; use stage4-final-gate.yml"
